--- src/sdk/objc/components/audio/RTCAudioSessionConfiguration.m	2025-09-01 12:15:08
+++ src/sdk/objc/components/audio/RTCAudioSessionConfiguration.m	2025-09-08 14:59:21
@@ -56,14 +56,19 @@
   self = [super init];
   if (self) {
     // Use a category which supports simultaneous recording and playback.
-    // By default, using this category implies that our app’s audio is
+    // By default, using this category implies that our app's audio is
     // nonmixable, hence activating the session will interrupt any other
     // audio sessions which are also nonmixable.
+#if TARGET_OS_SIMULATOR
+    _category = AVAudioSessionCategoryPlayback;
+    _mode = AVAudioSessionModeDefault;
+    _categoryOptions = 0;
+#else
     _category = AVAudioSessionCategoryPlayAndRecord;
-    _categoryOptions = AVAudioSessionCategoryOptionAllowBluetooth;
-
-    // Specify mode for two-way voice communication (e.g. VoIP).
     _mode = AVAudioSessionModeVoiceChat;
+    _categoryOptions = AVAudioSessionCategoryOptionAllowBluetooth |
+                      AVAudioSessionCategoryOptionDefaultToSpeaker;
+#endif
 
     // Use best sample rate and buffer duration if the CPU has more than one
     // core.
